/** @type {import('next').NextConfig} */

const nextConfig = {
    reactStrictMode: true,
    output: "export",
    // basePath: "/hanbin",
    images: {
        unoptimized: true
    }
};

export default nextConfig;
