/** @type {import('next').NextConfig} */

const nextConfig = {
    reactStrictMode: true,
    output: "export",
    assetsDir: "/hanbin/",
    images: {
        unoptimized: true
    }
};

export default nextConfig;
